# dotfiles agent guide

Read `README.md` first.

Keep tracked files safe to publish. Do not add credentials, generated state,
machine inventories, job-specific config, private paths, private agent policy,
sessions, logs, caches, or databases.

Each root folder owns its setup notes in its own `README.md`.
