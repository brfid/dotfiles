# yazi

Yazi uses a balanced three-pane layout, natural sorting, wrapped previews, and `?`/`~` help bindings.

Live paths:
- `yazi.toml` -> `~/.config/yazi/yazi.toml`
- `keymap.toml` -> `~/.config/yazi/keymap.toml`
