# /etc/samba/smb.conf for Raspberry Pi source share
# Reference only — review and apply manually.

[global]
   workgroup = WORKGROUP
   server string = jean-claude
   server role = standalone server
   security = user
   map to guest = Never

   disable netbios = yes
   smb ports = 445

   server min protocol = SMB2_10
   log file = /var/log/samba/log.%m
   max log size = 1000
   logging = file

   # Limit access to localhost and the Tailscale tailnet.
   hosts allow = 127.0.0.1 100.64.0.0/10 fd7a:115c:a1e0::/48
   hosts deny = 0.0.0.0/0 ::/0

[src]
   comment = Source Tree
   path = /home/__USER__/src
   browseable = yes
   read only = no
   guest ok = no
   valid users = __USER__
   force user = __USER__
   create mask = 0644
   directory mask = 0755

   # Apple clients expect these metadata/resource-fork behaviors.
   vfs objects = catia fruit streams_xattr
   fruit:metadata = stream
   fruit:resource = stream

   hide unreadable = yes
   hide files = /.git/.venv/.direnv/.pytest_cache/.mypy_cache/.ruff_cache/node_modules/.DS_Store/._*/
   veto files = /.git/.venv/.direnv/.pytest_cache/.mypy_cache/.ruff_cache/node_modules/.DS_Store/._*/
   delete veto files = yes
