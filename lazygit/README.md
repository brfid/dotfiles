# lazygit

Role: optional terminal Git UI.

Facts:
- LazyGit is available, but the live config is currently empty.
- Keep this directory as an intentional-empty marker until keybindings, paging,
  or UI preferences become durable.

Live path:
- `~/.config/lazygit/config.yml`
